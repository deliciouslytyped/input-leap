# This file was mostly generated by an LLM.

# InputLeap -- mouse and keyboard sharing utility
# Copyright (C) 2026 InputLeap contributors

set(sources MSWindowsDropHelper.cpp)

#TODO why this and not WIN32?
if (BUILD_MSWINDOWS)
    list(APPEND sources
        input-leapdh.exe.manifest
    )
endif()

add_executable(input-leapdh WIN32 ${sources})

target_link_libraries(input-leapdh
    ole32
    shell32
)
    
# Install alongside other executables
install(TARGETS input-leapdh DESTINATION .)
